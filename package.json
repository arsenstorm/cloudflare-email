{
  "name": "hono-email-worker",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "dependencies": {
    "hono": "^4.7.11",
    "postal-mime": "^2.4.3"
  },
  "devDependencies": {
    "@biomejs/biome": "1.9.4",
    "wrangler": "^4.4.0"
  }
}